[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = ">=3.12,<3.13"
numpy = "^2.2.2"
pytest = "^8.3.4"
haystack-ai = "^2.9.0"
python-dotenv = "^1.0.0"
trafilatura = "^2.0.0"



[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
